---
import BaseLayout from '../layouts/BaseLayout.astro';
import Greeting from '../components/Greeting';
const allPosts = Object.values(import.meta.glob('../posts/*.md', { eager: true }));
const pageTitle = "Astrazo";
---
<BaseLayout pageTitle={pageTitle}>
	<h1>Extraño a Karen</h1>
	<p>Este es la pagina principal aca bien bastarda</p>
	<Greeting client:load messages={["HOLAAA", "¿Como te va?", "BIENVENIDOS", "Gracias por estar aqui"]} />
</BaseLayout>